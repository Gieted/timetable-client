package pl.gieted.timetable.client;

import javafx.application.Application;
import pl.gieted.timetable.client.javafx.TimetableClient;

public class Main {
    public static void main(String[] args) {
        Application.launch(TimetableClient.class, args);
    }
}
